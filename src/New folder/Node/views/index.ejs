<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <h1>hello there 😀</h1>
    <p>Deep dive into node.js</p>
    <section>
      <% if (sections.length > 0) { %> <% sections.map((section) =>{%>
      <a style="color: darkslategrey" href="/sections/<%= section._id %>">
        <h3><%= section.title %></h3>
        <p><%= section.body %></p>
      </a>
      <%}) %> <% } else { %>
      <h3>No sections to display</h3>
      <% } %>
    </section>
  </body>
</html>
